1，接口中可以定义其他接口，实现接口嵌套

2，类中可以定义其他接口，并实现接口

3，在接口中定义接口，不能是private，因为接口中一切都是public

4，外部类，可以访问类内的接口

5，如果在一个类中，定义的接口是private的，则其他类不能直接访问，只能通过方法进行操作。
即使通过get，也返回不了，因为接口本身就是私有的，私有的本身就不能访问。

接口A {
    private 接口B
    getB() {
        new B
    }

    set(B){

    }
}

main{
    A.B = new A().getB  //不能返回，因为接口B本身是私有的，main方法无法访问
    new A().setB(new A().getB)  //是可以的，

}


其他知识，本章，就是适配器，工厂模式。

